<div class="container mt-4">
   <h3 class="mb-0"><%= @camera.brand %> - <%= @camera.model %></h3>

  <div class="card-body mt-4">
    <p><strong>Model:</strong> <%= @camera.model %></p>
    <p><strong>Description:</strong> <%= @camera.description %></p>
    <p><strong>Price:</strong> ¥<%= @camera.price %></p>
  </div>
  <div>
    <h4 class="mt-4">Reserve This Camera 📅</h4>
    <%= simple_form_for Reservation.new, url: reservations_path, method: :post do |f| %>
    <%= f.hidden_field :camera_id, value: @camera.id %>
    <%= f.input :start_date, as: :string, input_html: { data: { controller: 'datepicker' } } %>
    <%= f.submit "Reserve Camera", class: "btn btn-primary" %>
    <% end %>
  </div>

  <div class="card-footer d-flex justify-content-between mt-3">
    <%= link_to 'Back to home', cameras_path, class: "btn btn-secondary" %>
  <div>
</div>
